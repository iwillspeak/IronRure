mode: ContinuousDelivery
branches:
  master:
    regex: ^main$
    tag: beta
    increment: none
  feature:
    mode: ContinuousDeployment
    regex: ^(?!main).*$
    increment: none
ignore:
  sha: []
merge-message-formats: {}
